# Nothing is needed here any more.
#
# The database and the room objects are connected through bindings in
# wrangler.jsonc, not through a connection string.
# If you ever add a secret, add it here as a NAME (no value) and set the
# real value with:  npx wrangler secret put NAME
